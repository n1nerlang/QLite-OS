# Fonts

This directory stores lightweight font assets for QLite-OS.

Current asset:

- `qlite-5x7.font.txt` - a small bitmap font sample that can be turned into a kernel text renderer later.

Planned direction:

- Add a real binary bitmap font once the build pipeline supports asset packing.
- Use the same font for the launcher, terminal, and app surfaces so the UI feels consistent.
